import styled from "styled-components"

const Body = styled.div`
  max-width: 1200px;
  h1 {
    margin: 0;
  }
  .gatsby-resp-image-wrapper {
    margin: 0 !important;
  }
  sup {
    background: black;
    color: white;
    font-size: 1.4rem;
    padding: 0 0.3rem;
  }
`

export default Body
